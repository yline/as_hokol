<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hokol.test">

    <application
        android:name=".common.IApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Test -->
        <activity
            android:name=".TestInnerActivity"
            android:label="HoKol测试入口">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Test -->
        <activity android:name=".http.login.TestLoginActivity">
        </activity>
        <activity android:name=".http.news.TestNewsActivity"/>
        <activity android:name=".widget.ADActivity"/>
        <activity android:name=".widget.DropMenuActivity"/>
        <activity android:name=".widget.LabelLayoutActivity"/>
        <activity android:name=".http.dynamic.TestDynamicActivity"/>
        <activity android:name=".widget.RecycleActivity">
        </activity>
        <activity android:name=".widget.GlideActivity">
        </activity>
        <activity android:name=".widget.SecondaryActivity">
        </activity>
        <activity android:name=".widget.SwipeRefreshActivity">
        </activity>
        <activity android:name=".widget.ProgressBarActivity">
        </activity>
    </application>

</manifest>